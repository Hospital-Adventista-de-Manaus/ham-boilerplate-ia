# Uso local: docker compose up --build
# Em produção use Coolify (cria 2 "Applications" apontando para apps/web/Dockerfile e apps/api/Dockerfile)

services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    container_name: ham-web
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      INTERNAL_API_URL: http://api:3001
      NEXT_PUBLIC_API_URL: http://localhost:3001
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: ham-api
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      CORS_ORIGINS: http://localhost:3000
      # DATABASE_URL: postgresql://app:app@db:5432/app
    # depends_on:
    #   db:
    #     condition: service_healthy
    restart: unless-stopped

  # Descomente para incluir Postgres
  # db:
  #   image: postgres:16-alpine
  #   container_name: ham-db
  #   environment:
  #     POSTGRES_USER: app
  #     POSTGRES_PASSWORD: app
  #     POSTGRES_DB: app
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - db-data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U app"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   db-data:
